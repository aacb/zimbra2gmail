#ifndef GLOBALS_H_   /* Include guard */
#define GLOBALS_H_

#endif // GLOBALS_H_

// global functions
void boo(char *);

// global variables
extern char msg[200], lexeme[200], *symbols;
extern wint_t ch, chLast;
extern int token;
extern FILE *fp;

